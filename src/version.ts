/**
 * shia2n-mcp-core の版番号は、このファイル 1 か所だけに置く。
 *
 * 元の shia2n-mcp では版番号が 2 か所にあり、点検の口が実物より 25 古い
 * 番号を返していた（v0.44.0 で 1 か所に統合）。同じことを繰り返さないため、
 * 最初からここだけに置く。
 *
 * 版を上げるときは、この 1 行だけを書き換える。
 * 何を変えたかは index.ts の先頭の履歴に足す。
 */
export const APP_VERSION = "0.1.0";
